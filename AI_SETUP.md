# Инструкция по подключению AI провайдеров

Это руководство поможет вам настроить подключение к различным AI провайдерам для вашего приложения.

## 📋 Содержание

1. [OpenAI (GPT-4)](#openai-gpt-4)
2. [Google Gemini](#google-gemini)
3. [Anthropic Claude](#anthropic-claude)
4. [Groq (Llama)](#groq-llama)
5. [Mistral AI](#mistral-ai)
6. [Настройка переменных окружения](#настройка-переменных-окружения)
7. [Развертывание](#развертывание)

---

## 🤖 OpenAI (GPT-4)

### Получение API ключа:

1. Перейдите на [platform.openai.com](https://platform.openai.com)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **API Keys**
4. Нажмите **Create new secret key**
5. Скопируйте ключ (он показывается только один раз!)

### Модели:
- `gpt-4-turbo-preview` - самая мощная модель (используется по умолчанию)
- `gpt-4` - стандартная GPT-4
- `gpt-3.5-turbo` - более быстрая и дешевая модель

### Цены:
- GPT-4 Turbo: ~$0.01 за 1K токенов (вход) / ~$0.03 за 1K токенов (выход)
- GPT-3.5 Turbo: ~$0.0005 за 1K токенов (вход) / ~$0.0015 за 1K токенов (выход)

### Переменная окружения:
```bash
OPENAI_API_KEY=sk-...
```

---

## 🌟 Google Gemini

### Получение API ключа:

1. Перейдите на [makersuite.google.com/app/apikey](https://makersuite.google.com/app/apikey)
2. Войдите с Google аккаунтом
3. Нажмите **Create API Key**
4. Выберите проект или создайте новый
5. Скопируйте ключ

### Модели:
- `gemini-pro` - основная модель (используется по умолчанию)
- `gemini-pro-vision` - с поддержкой изображений

### Цены:
- Бесплатно до 60 запросов в минуту
- После: $0.00025 за 1K токенов (вход) / $0.0005 за 1K токенов (выход)

### Переменная окружения:
```bash
GEMINI_API_KEY=...
```

---

## 🧠 Anthropic Claude

### Получение API ключа:

1. Перейдите на [console.anthropic.com](https://console.anthropic.com)
2. Зарегистрируйтесь или войдите
3. Перейдите в **API Keys**
4. Нажмите **Create Key**
5. Скопируйте ключ

### Модели:
- `claude-3-opus-20240229` - самая мощная (используется по умолчанию)
- `claude-3-sonnet-20240229` - баланс скорости и качества
- `claude-3-haiku-20240307` - быстрая и дешевая

### Цены:
- Claude 3 Opus: $15 за 1M токенов (вход) / $75 за 1M токенов (выход)
- Claude 3 Sonnet: $3 за 1M токенов (вход) / $15 за 1M токенов (выход)
- Claude 3 Haiku: $0.25 за 1M токенов (вход) / $1.25 за 1M токенов (выход)

### Переменная окружения:
```bash
ANTHROPIC_API_KEY=sk-ant-...
```

---

## ⚡ Groq (Llama)

### Получение API ключа:

1. Перейдите на [console.groq.com](https://console.groq.com)
2. Зарегистрируйтесь или войдите
3. Перейдите в **API Keys**
4. Нажмите **Create API Key**
5. Скопируйте ключ

### Модели:
- `llama-3.1-70b-versatile` - Llama 3.1 70B (используется по умолчанию)
- `llama-3.1-8b-instant` - Llama 3.1 8B (быстрее)
- `llama-3-8b-8192` - Llama 3 8B (старая версия)
- `mixtral-8x7b-32768` - Mixtral модель

### Цены:
- **Бесплатно!** Очень быстрые ответы благодаря специализированному железу

### Переменная окружения:
```bash
GROQ_API_KEY=gsk_...
```

---

## 🎯 Mistral AI

### Получение API ключа:

1. Перейдите на [console.mistral.ai](https://console.mistral.ai)
2. Зарегистрируйтесь или войдите
3. Перейдите в **API Keys**
4. Нажмите **Create new key**
5. Скопируйте ключ

### Модели:
- `mistral-large-latest` - самая мощная (используется по умолчанию)
- `mistral-medium-latest` - баланс
- `mistral-small-latest` - быстрая

### Цены:
- Mistral Large: €2 за 1M токенов (вход) / €6 за 1M токенов (выход)
- Mistral Medium: €0.5 за 1M токенов (вход) / €1.5 за 1M токенов (выход)
- Mistral Small: €0.1 за 1M токенов (вход) / €0.3 за 1M токенов (выход)

### Переменная окружения:
```bash
MISTRAL_API_KEY=...
```

---

## ⚙️ Настройка переменных окружения

### Локальная разработка:

1. Создайте файл `.env` в папке `backend/`:
```bash
cd backend
touch .env
```

2. Добавьте ваши API ключи в файл `.env`:
```bash
# OpenAI
OPENAI_API_KEY=sk-your-openai-key-here

# Google Gemini
GEMINI_API_KEY=your-gemini-key-here

# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-your-claude-key-here

# Groq
GROQ_API_KEY=gsk_your-groq-key-here

# Mistral AI
MISTRAL_API_KEY=your-mistral-key-here
```

3. Убедитесь, что файл `.env` добавлен в `.gitignore`:
```
backend/.env
```

### Railway (развертывание):

1. Перейдите в ваш проект на [railway.app](https://railway.app)
2. Откройте **Variables** в настройках проекта
3. Добавьте переменные окружения:
   - `OPENAI_API_KEY`
   - `GEMINI_API_KEY`
   - `ANTHROPIC_API_KEY`
   - `GROQ_API_KEY`
   - `MISTRAL_API_KEY`

### Vercel (развертывание):

1. Перейдите в ваш проект на [vercel.com](https://vercel.com)
2. Откройте **Settings** → **Environment Variables**
3. Добавьте переменные окружения для Production, Preview и Development

---

## 🚀 Развертывание

### Установка зависимостей:

```bash
cd backend
pip install -r requirements.txt
```

### Запуск локально:

```bash
python server.py
```

Сервер запустится на `http://localhost:8000`

### Запуск с Gunicorn (продакшн):

```bash
gunicorn -w 4 -b 0.0.0.0:8000 server:app
```

### Проверка работы:

```bash
curl http://localhost:8000/api/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": 1234567890.123,
  "version": "1.0.0"
}
```

---

## 🔒 Безопасность

⚠️ **ВАЖНО:**

1. **Никогда не коммитьте API ключи в Git!**
2. Используйте переменные окружения
3. Ограничьте доступ к API ключам
4. Регулярно ротируйте ключи
5. Установите лимиты использования в настройках провайдеров

---

## 📊 Сравнение провайдеров

| Провайдер | Скорость | Качество | Цена | Лучше для |
|-----------|----------|----------|------|-----------|
| **OpenAI GPT-4** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 💰💰💰 | Сложные задачи, анализ |
| **Google Gemini** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 💰💰 | Креативность, мультимодальность |
| **Anthropic Claude** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 💰💰💰 | Длинные тексты, безопасность |
| **Groq (Llama)** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 🆓 | Быстрые ответы, бесплатно |
| **Mistral AI** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 💰💰 | Многоязычность, Европа |

---

## 🆘 Решение проблем

### Ошибка "API key not configured"

- Проверьте, что переменная окружения установлена
- Убедитесь, что файл `.env` находится в правильной папке
- Перезапустите сервер после изменения `.env`

### Ошибка "Module not found"

```bash
pip install -r requirements.txt
```

### Ошибка подключения к API

- Проверьте интернет-соединение
- Убедитесь, что API ключ действителен
- Проверьте лимиты использования на сайте провайдера

---

## 📚 Дополнительные ресурсы

- [OpenAI Documentation](https://platform.openai.com/docs)
- [Google Gemini Documentation](https://ai.google.dev/docs)
- [Anthropic Claude Documentation](https://docs.anthropic.com)
- [Groq Documentation](https://console.groq.com/docs)
- [Mistral AI Documentation](https://docs.mistral.ai)

---

**Удачи с настройкой! 🚀**