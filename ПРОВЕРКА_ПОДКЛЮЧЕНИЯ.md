# 🔌 Проверка подключения Frontend ↔ Backend

Это руководство поможет вам проверить, общаются ли между собой ваши фронтенд и бекенд сервера.

## 📋 Быстрая проверка

### Вариант 1: Использование HTML тестера (рекомендуется)

1. Откройте файл `test_connection.html` в браузере
2. Укажите URL вашего backend сервера (например: `http://localhost:8000`)
3. Нажмите "🚀 Запустить все тесты"
4. Проверьте результаты

### Вариант 2: Использование Python скрипта

```bash
# Установите зависимости (если еще не установлены)
pip install requests

# Запустите тест
python test_connection.py --url http://localhost:8000 --api-path /api
```

### Вариант 3: Ручная проверка через curl

```bash
# 1. Health Check
curl http://localhost:8000/api/health

# 2. Providers
curl http://localhost:8000/api/providers

# 3. Chat (тестовое сообщение)
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"Привет","provider":"openai","temperature":0.7,"maxTokens":100}'
```

## 🔍 Что проверяется

### 1. Health Check (`/api/health`)
- ✅ Сервер запущен и отвечает
- ✅ Endpoint доступен
- ✅ Возвращает корректный JSON

### 2. Providers (`/api/providers`)
- ✅ Endpoint доступен
- ✅ Возвращает список провайдеров
- ✅ Показывает статус каждого провайдера (настроен/не настроен)

### 3. Chat (`/api/chat`)
- ✅ Endpoint принимает POST запросы
- ✅ Обрабатывает сообщения
- ⚠️ Может показать предупреждение, если API ключи не настроены (это нормально)

### 4. CORS
- ✅ Проверяет наличие CORS заголовков
- ✅ Важно для работы фронтенда с другого домена

## 🛠️ Настройка подключения

### Для локальной разработки

1. **Backend** должен быть запущен на `http://localhost:8000` (или другом порту)
2. **Frontend** должен быть настроен на использование этого URL

В файле `frontend/telegram-config.js`:
```javascript
API_BASE_URL: 'http://localhost:8000/api'
```

### Для продакшена

1. Убедитесь, что backend доступен по публичному URL
2. Настройте `API_BASE_URL` в `frontend/telegram-config.js`:
```javascript
API_BASE_URL: 'https://your-backend-domain.com/api'
```

## 🐛 Решение проблем

### Ошибка: "Failed to fetch" / "Connection refused"

**Причины:**
- Backend сервер не запущен
- Неправильный URL в конфигурации
- Проблемы с CORS

**Решение:**
1. Проверьте, запущен ли backend:
   ```bash
   cd backend
   python server.py
   ```

2. Проверьте URL в `frontend/telegram-config.js`

3. Убедитесь, что CORS настроен в backend (должно быть `CORS(app)` в `server.py`)

### Ошибка: "API key not configured"

**Это нормально!** Это означает, что:
- ✅ Backend работает
- ✅ Endpoints доступны
- ⚠️ API ключи не настроены (для теста подключения это не критично)

Для полной проверки настройте API ключи в `.env` файле backend.

### Ошибка: CORS

**Причины:**
- Frontend и Backend на разных доменах
- CORS не настроен в backend

**Решение:**
Убедитесь, что в `backend/server.py` есть:
```python
from flask_cors import CORS
CORS(app)
```

## 📊 Интерпретация результатов

### ✅ Все тесты пройдены
Отлично! Frontend и Backend общаются корректно.

### ⚠️ Некоторые тесты не пройдены
- Если Health Check пройден, но Chat не работает → проверьте API ключи
- Если Health Check не пройден → проверьте, запущен ли сервер

### ❌ Все тесты провалены
- Проверьте, запущен ли backend сервер
- Проверьте URL в конфигурации
- Проверьте логи backend сервера

## 🔗 Полезные ссылки

- Backend сервер: `backend/server.py`
- Frontend API клиент: `frontend/api.js`
- Конфигурация: `frontend/telegram-config.js`

## 💡 Советы

1. **Всегда проверяйте Health Check первым** - это самый простой способ убедиться, что сервер работает

2. **Используйте консоль браузера** (F12) для просмотра сетевых запросов и ошибок

3. **Проверяйте логи backend** при возникновении проблем

4. **Для продакшена** убедитесь, что:
   - Backend доступен по HTTPS
   - CORS настроен правильно
   - API ключи настроены в переменных окружения
