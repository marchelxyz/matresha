# 📘 Инструкция по подключению ChatGPT (GPT-4) к приложению

Подробное руководство по настройке и подключению OpenAI GPT-4 к вашему AI-ассистенту.

---

## 🎯 Шаг 1: Получение API ключа OpenAI

### 1.1 Регистрация на OpenAI

1. Перейдите на сайт [platform.openai.com](https://platform.openai.com)
2. Нажмите **Sign Up** (Регистрация) или **Log In** (Вход), если у вас уже есть аккаунт
3. Заполните форму регистрации или войдите через Google/Microsoft

### 1.2 Создание API ключа

1. После входа перейдите в раздел **API Keys** (Ключи API)
   - Ссылка: [platform.openai.com/api-keys](https://platform.openai.com/api-keys)
2. Нажмите кнопку **+ Create new secret key** (Создать новый секретный ключ)
3. Введите название ключа (например, "Telegram AI Assistant")
4. Нажмите **Create secret key**
5. ⚠️ **ВАЖНО:** Скопируйте ключ немедленно! Он начинается с `sk-` и показывается только один раз
6. Сохраните ключ в безопасном месте

### 1.3 Пополнение баланса (если необходимо)

1. Перейдите в раздел **Billing** (Биллинг)
2. Нажмите **Add payment method** (Добавить способ оплаты)
3. Добавьте карту для оплаты
4. Установите лимиты расходов (рекомендуется для безопасности)

---

## 🔧 Шаг 2: Настройка локального окружения

### 2.1 Создание файла .env

1. Откройте папку `backend/` в вашем проекте
2. Скопируйте файл `.env.example` в `.env`:
   ```bash
   cd backend
   cp .env.example .env
   ```

### 2.2 Добавление API ключа

Откройте файл `backend/.env` в текстовом редакторе и добавьте ваш ключ:

```bash
# OpenAI (GPT-4)
OPENAI_API_KEY=sk-ваш-ключ-здесь
```

**Пример:**
```bash
OPENAI_API_KEY=sk-proj-abc123xyz789...
```

### 2.3 Проверка .gitignore

Убедитесь, что файл `.env` добавлен в `.gitignore`, чтобы не закоммитить ключ:

```bash
# Проверьте содержимое .gitignore
cat .gitignore | grep .env
```

Если `.env` не в списке, добавьте:
```
backend/.env
```

---

## 📦 Шаг 3: Установка зависимостей

### 3.1 Установка Python пакетов

```bash
cd backend
pip install -r requirements.txt
```

Или если используете виртуальное окружение:

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

### 3.2 Проверка установки

Убедитесь, что пакет `openai` установлен:

```bash
pip list | grep openai
```

Должно показать что-то вроде:
```
openai                         1.12.0
```

---

## 🚀 Шаг 4: Запуск сервера

### 4.1 Локальный запуск

```bash
cd backend
python server.py
```

Сервер запустится на `http://localhost:8000`

### 4.2 Проверка работы

Откройте новый терминал и проверьте:

```bash
curl http://localhost:8000/api/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "timestamp": 1234567890.123,
  "version": "1.0.0"
}
```

### 4.3 Проверка доступности GPT

```bash
curl http://localhost:8000/api/providers
```

В ответе должен быть `"openai"` в списке доступных провайдеров:

```json
{
  "success": true,
  "data": {
    "providers": ["openai", ...],
    "status": {
      "openai": {
        "available": true,
        "has_key": true
      }
    }
  }
}
```

---

## 🌐 Шаг 5: Настройка для продакшена (Railway)

### 5.1 Добавление переменной окружения на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Откройте ваш проект
3. Перейдите в раздел **Variables** (Переменные)
4. Нажмите **+ New Variable** (Новая переменная)
5. Добавьте:
   - **Name:** `OPENAI_API_KEY`
   - **Value:** ваш API ключ (начинается с `sk-`)
6. Нажмите **Add**

### 5.2 Перезапуск сервиса

После добавления переменной:
1. Перейдите в раздел **Deployments** (Развертывания)
2. Нажмите **Redeploy** (Переразвернуть) или подождите автоматического перезапуска

### 5.3 Проверка на продакшене

После деплоя проверьте:

```bash
curl https://ваш-домен.railway.app/api/providers
```

---

## 🧪 Шаг 6: Тестирование подключения

### 6.1 Тест через API

Отправьте тестовый запрос:

```bash
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Привет! Как дела?",
    "provider": "openai",
    "temperature": 0.7,
    "maxTokens": 2000
  }'
```

Должен вернуться ответ от GPT-4.

### 6.2 Тест через фронтенд

1. Откройте `frontend/index.html` в браузере
2. Убедитесь, что в настройках выбран провайдер **GPT-4**
3. Отправьте тестовое сообщение
4. Должен прийти ответ от GPT-4

---

## ⚙️ Шаг 7: Настройка модели GPT

### 7.1 Доступные модели

В файле `backend/server.py` можно изменить модель GPT:

```python
# Текущая модель (строка 67):
model="gpt-4-turbo-preview"

# Доступные модели:
# - "gpt-4-turbo-preview" - самая мощная (по умолчанию)
# - "gpt-4" - стандартная GPT-4
# - "gpt-3.5-turbo" - быстрая и дешевая
```

### 7.2 Изменение модели

Откройте `backend/server.py` и найдите класс `OpenAIProvider`:

```python
# Строка 66-67
response = self.client.chat.completions.create(
    model="gpt-4-turbo-preview",  # Измените здесь
    ...
)
```

Замените на нужную модель, например:
```python
model="gpt-3.5-turbo"  # Для более быстрых и дешевых ответов
```

---

## 💰 Шаг 8: Понимание стоимости

### 8.1 Тарифы OpenAI

- **GPT-4 Turbo:**
  - Вход: ~$0.01 за 1,000 токенов
  - Выход: ~$0.03 за 1,000 токенов

- **GPT-3.5 Turbo:**
  - Вход: ~$0.0005 за 1,000 токенов
  - Выход: ~$0.0015 за 1,000 токенов

### 8.2 Пример расчета

Одно сообщение ~500 токенов:
- GPT-4 Turbo: ~$0.02 за сообщение
- GPT-3.5 Turbo: ~$0.001 за сообщение

### 8.3 Установка лимитов

1. Перейдите на [platform.openai.com/account/billing/limits](https://platform.openai.com/account/billing/limits)
2. Установите **Hard limit** (жесткий лимит) для безопасности
3. Рекомендуется установить лимит $10-50 в месяц для начала

---

## 🔍 Шаг 9: Решение проблем

### Проблема: "OpenAI API key not configured"

**Решение:**
1. Проверьте, что файл `.env` существует в папке `backend/`
2. Убедитесь, что ключ начинается с `sk-`
3. Перезапустите сервер после изменения `.env`
4. Проверьте, что нет лишних пробелов в ключе

### Проблема: "Module 'openai' not found"

**Решение:**
```bash
cd backend
pip install openai==1.12.0
```

### Проблема: "Insufficient quota" или "Rate limit exceeded"

**Решение:**
1. Проверьте баланс на [platform.openai.com/account/billing](https://platform.openai.com/account/billing)
2. Убедитесь, что у вас есть доступ к API (может потребоваться пополнение)
3. Проверьте лимиты использования

### Проблема: "Invalid API key"

**Решение:**
1. Убедитесь, что ключ скопирован полностью (без пробелов)
2. Проверьте, что ключ не истек
3. Создайте новый ключ, если старый не работает

### Проблема: Сервер не запускается

**Решение:**
1. Проверьте, что порт 8000 свободен:
   ```bash
   lsof -i :8000  # Linux/Mac
   netstat -ano | findstr :8000  # Windows
   ```
2. Измените порт в `server.py`:
   ```python
   port = int(os.environ.get('PORT', 8000))  # Измените 8000 на другой порт
   ```

---

## ✅ Шаг 10: Проверочный чеклист

Пройдитесь по списку, чтобы убедиться, что всё настроено:

- [ ] API ключ получен с platform.openai.com
- [ ] Файл `.env` создан в папке `backend/`
- [ ] Ключ добавлен в `.env` как `OPENAI_API_KEY=sk-...`
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Сервер запускается без ошибок
- [ ] `/api/providers` показывает `openai` как доступный
- [ ] Тестовый запрос возвращает ответ от GPT
- [ ] Фронтенд может отправлять сообщения и получать ответы
- [ ] Для продакшена: переменная добавлена на Railway
- [ ] Лимиты расходов установлены на OpenAI

---

## 📚 Дополнительные ресурсы

- [OpenAI API Документация](https://platform.openai.com/docs)
- [OpenAI Python SDK](https://github.com/openai/openai-python)
- [OpenAI Pricing](https://openai.com/pricing)
- [OpenAI Status](https://status.openai.com)

---

## 🎉 Готово!

Теперь ваш чат GPT подключен и готов к работе! Вы можете:

- Отправлять сообщения через фронтенд
- Использовать GPT-4 для ответов
- Настраивать температуру и количество токенов
- Сохранять историю разговоров

**Удачи! 🚀**
