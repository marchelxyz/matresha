# Business Assistant - Frontend

Frontend приложение для Business Assistant Telegram Mini App.

## 🚀 Развертывание на Vercel

1. **Подключите репозиторий к Vercel**
   - Зайдите на [Vercel](https://vercel.com)
   - Импортируйте ваш Git репозиторий
   - В настройках проекта укажите:
     - **Root Directory**: `frontend`
     - **Framework Preset**: Other
     - **Build Command**: (оставьте пустым или `npm run build`)
     - **Output Directory**: `.` (текущая директория)

2. **Автоматическое развертывание**
   - Vercel автоматически определит статические файлы
   - После деплоя вы получите URL вашего приложения

3. **Обновление конфигурации**
   - Обновите `API_BASE_URL` в `telegram-config.js` на URL вашего backend сервера
   - Обновите `WEB_APP_URL` на URL вашего Vercel приложения

## 📁 Структура

```
frontend/
├── index.html           # Главная страница
├── styles.css          # Стили
├── script.js           # Основная логика
├── api.js              # API клиент
├── telegram-config.js  # Конфигурация Telegram
├── manifest.json       # PWA манифест
├── robots.txt          # SEO конфигурация
├── sitemap.xml         # Карта сайта
├── package.json        # Node.js зависимости
└── vercel.json         # Конфигурация Vercel
```

## 🔧 Локальная разработка

```bash
cd frontend
npm install
npm run dev
```

Приложение будет доступно на `http://localhost:3000` (или другом порту, который выберет serve)

## ⚙️ Конфигурация

Обновите следующие файлы перед развертыванием:

1. **telegram-config.js**
   ```javascript
   API_BASE_URL: 'https://your-railway-backend.railway.app/api',
   WEB_APP_URL: 'https://your-vercel-app.vercel.app'
   ```

2. **api.js** (если нужно)
   ```javascript
   this.baseURL = 'https://your-railway-backend.railway.app/api';
   ```
