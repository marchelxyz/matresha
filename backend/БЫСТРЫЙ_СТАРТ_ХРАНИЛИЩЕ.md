# 🚀 Быстрый старт: Настройка Yandex Object Storage

## Вариант 1: Автоматическая настройка (рекомендуется)

Запустите интерактивный скрипт:

```bash
cd backend
python3 setup_storage.py
```

Скрипт попросит ввести:
- Access Key ID
- Secret Access Key  
- Имя бакета
- Регион (по умолчанию: ru-central1)

После настройки автоматически запустится проверка подключения.

## Вариант 2: Ручная настройка

### 1. Создайте файл .env

```bash
cd backend
cp .env.example .env
```

### 2. Откройте .env и заполните переменные:

```env
YANDEX_STORAGE_ENDPOINT=https://storage.yandexcloud.net
YANDEX_STORAGE_ACCESS_KEY=ваш-access-key-id
YANDEX_STORAGE_SECRET_KEY=ваш-secret-access-key
YANDEX_STORAGE_BUCKET=имя-вашего-бакета
YANDEX_STORAGE_REGION=ru-central1
```

### 3. Проверьте подключение:

```bash
python3 test_yandex_storage.py
```

## 📋 Что нужно получить в Yandex Cloud

1. **Access Key ID** и **Secret Access Key**:
   - Консоль → IAM → Сервисные аккаунты → Создать аккаунт
   - Назначить роль `storage.editor`
   - IAM → Ключи → Создать статический ключ

2. **Бакет (Bucket)**:
   - Консоль → Object Storage → Бакеты → Создать бакет
   - Имя должно быть уникальным (например: `my-app-files-2024`)
   - Регион: `ru-central1`

## ✅ Проверка работы

После настройки запустите:

```bash
python3 test_yandex_storage.py
```

Вы должны увидеть:
```
✓ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ УСПЕШНО!
  Yandex Object Storage готов к использованию
```

## 📚 Дополнительная информация

- 📖 [Подробная инструкция](YANDEX_STORAGE_SETUP.md)
- 🔧 [Руководство по настройке](НАСТРОЙКА_ХРАНИЛИЩА.md)
- 🌐 [Документация Yandex](https://cloud.yandex.ru/docs/storage/)

## ❓ Проблемы?

Если что-то не работает:

1. Проверьте переменные окружения: `python3 test_yandex_storage.py`
2. Убедитесь, что бакет создан в консоли Yandex Cloud
3. Проверьте права доступа сервисного аккаунта
4. Убедитесь, что ключи доступа правильные

## 💡 Что дальше?

После успешной настройки все загруженные файлы будут автоматически сохраняться в Yandex Object Storage и получать временные ссылки (действительны 7 дней).
