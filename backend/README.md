# Business Assistant - Backend

Backend сервер для Business Assistant Telegram Mini App.

## 🚀 Развертывание на Railway

1. **Подключите репозиторий к Railway**
   - Зайдите на [Railway](https://railway.app)
   - Создайте новый проект
   - Подключите ваш Git репозиторий
   - Выберите директорию `backend` как root директорию

2. **Настройка переменных окружения**
   - Railway автоматически определит Python проект
   - Порт будет установлен автоматически через переменную `PORT`

3. **Запуск**
   - Railway автоматически запустит сервер используя `Procfile`
   - Сервер будет доступен по URL, предоставленному Railway

## 📁 Структура

```
backend/
├── server.py          # Основной сервер
├── requirements.txt   # Python зависимости
├── Procfile          # Конфигурация для Railway
└── api/              # API endpoints
    ├── operations/   # Операционные endpoints
    ├── marketing/    # Маркетинговые endpoints
    ├── accounting/   # Бухгалтерские endpoints
    ├── legal/        # Юридические endpoints
    └── ai/           # AI endpoints
```

## 🔧 Локальная разработка

```bash
cd backend
python server.py
```

Сервер будет доступен на `http://localhost:8000`

## 🌐 API Endpoints

- `GET /api/health` - Проверка здоровья сервера
- `POST /api/ai/chat` - AI чат
- `POST /api/operations/*` - Операционные endpoints
- `POST /api/marketing/*` - Маркетинговые endpoints
- `POST /api/accounting/*` - Бухгалтерские endpoints
- `POST /api/legal/*` - Юридические endpoints
