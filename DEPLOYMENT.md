# Инструкция по развертыванию

Этот документ содержит подробные инструкции по развертыванию проекта на Vercel (frontend) и Railway (backend).

## 📋 Предварительные требования

- Аккаунт на [Vercel](https://vercel.com)
- Аккаунт на [Railway](https://railway.app)
- Git репозиторий с вашим кодом

## 🎨 Развертывание Frontend на Vercel

### Шаг 1: Подключение репозитория

1. Войдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите "Add New Project"
3. Импортируйте ваш Git репозиторий
4. Выберите репозиторий и нажмите "Import"

### Шаг 2: Настройка проекта

В настройках проекта укажите:

- **Framework Preset**: `Other` или `Vite` (если используете)
- **Root Directory**: `frontend` ⚠️ **ВАЖНО!**
- **Build Command**: (оставьте пустым или `npm run build`)
- **Output Directory**: `.` (точка - текущая директория)
- **Install Command**: `npm install` (если нужно)

### Шаг 3: Переменные окружения (опционально)

Если нужно, добавьте переменные окружения в настройках проекта:
- `NEXT_PUBLIC_API_URL` - URL вашего backend API (если используете)

### Шаг 4: Деплой

1. Нажмите "Deploy"
2. Дождитесь завершения деплоя
3. Скопируйте URL вашего приложения (например: `https://your-app.vercel.app`)

### Шаг 5: Обновление конфигурации

После получения URL от Vercel:

1. Откройте `frontend/telegram-config.js`
2. Обновите `WEB_APP_URL`:
   ```javascript
   WEB_APP_URL: 'https://your-app.vercel.app'
   ```
3. Закоммитьте изменения и запушьте в репозиторий
4. Vercel автоматически передеплоит проект

## 🚂 Развертывание Backend на Railway

### Шаг 1: Подключение репозитория

1. Войдите в [Railway Dashboard](https://railway.app/dashboard)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий

### Шаг 2: Настройка Root Directory

⚠️ **ВАЖНО!** Railway не поддерживает установку root директории напрямую в UI, но можно использовать один из методов:

#### Метод 1: Использование railway.json (рекомендуется)

Railway автоматически прочитает `backend/railway.json` если он находится в корне проекта. Но так как наш файл находится в `backend/`, нужно:

1. После создания проекта, Railway попытается определить тип проекта
2. Если он определит неправильную директорию, используйте метод 2

#### Метод 2: Использование переменной окружения

1. В настройках проекта Railway
2. Перейдите в "Variables"
3. Добавьте переменную:
   - **Name**: `RAILWAY_SOURCE_DIR`
   - **Value**: `backend`

#### Метод 3: Использование .railwayignore (альтернатива)

Создайте файл `.railwayignore` в корне проекта:
```
frontend/
.git/
node_modules/
```

### Шаг 3: Настройка Python

Railway автоматически определит Python проект по наличию:
- `requirements.txt`
- `Procfile`
- `server.py`

### Шаг 4: Переменные окружения

Railway автоматически установит:
- `PORT` - порт для сервера (не нужно устанавливать вручную)

Если нужны дополнительные переменные:
- `BOT_TOKEN` - токен Telegram бота (если используется)
- `API_KEY` - ключи API (если используются)

### Шаг 5: Деплой

1. Railway автоматически начнет деплой после подключения репозитория
2. Дождитесь завершения деплоя
3. Скопируйте URL вашего сервиса (например: `https://your-app.railway.app`)

### Шаг 6: Обновление Frontend конфигурации

После получения URL от Railway:

1. Откройте `frontend/telegram-config.js`
2. Обновите `API_BASE_URL`:
   ```javascript
   API_BASE_URL: 'https://your-app.railway.app/api'
   ```

3. Откройте `frontend/api.js`
4. Обновите `baseURL`:
   ```javascript
   this.baseURL = 'https://your-app.railway.app/api';
   ```

5. Закоммитьте изменения и запушьте в репозиторий
6. Vercel автоматически передеплоит frontend

## 🔄 Обновление после изменений

### Frontend (Vercel)
- Автоматически деплоится при push в основную ветку
- Можно настроить деплой из других веток в настройках проекта

### Backend (Railway)
- Автоматически деплоится при push в основную ветку
- Можно настроить деплой из других веток в настройках проекта

## 🐛 Решение проблем

### Vercel не видит файлы frontend

**Проблема**: Vercel деплоит корневую директорию вместо `frontend/`

**Решение**: 
1. Проверьте, что в настройках проекта указан Root Directory: `frontend`
2. Убедитесь, что файл `vercel.json` находится в директории `frontend/`

### Railway не видит файлы backend

**Проблема**: Railway пытается деплоить корневую директорию или frontend

**Решение**:
1. Используйте переменную окружения `RAILWAY_SOURCE_DIR=backend`
2. Или создайте `.railwayignore` в корне проекта
3. Убедитесь, что `Procfile` и `requirements.txt` находятся в `backend/`

### Backend не запускается на Railway

**Проблема**: Ошибка при запуске сервера

**Решение**:
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что `Procfile` содержит правильную команду: `web: python server.py`
3. Проверьте, что Python версия совместима (Railway использует Python 3.8+)

### CORS ошибки

**Проблема**: Frontend не может подключиться к backend из-за CORS

**Решение**:
1. Убедитесь, что в `backend/server.py` настроены CORS заголовки
2. Проверьте, что `API_BASE_URL` в frontend указывает на правильный URL Railway

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel/Railway Dashboard
2. Убедитесь, что все конфигурационные файлы на месте
3. Проверьте, что переменные окружения установлены правильно
